// PDF.js Worker Placeholder
// This file is a placeholder. For a production app, you would need to:
// 1. Download the actual pdf.worker.min.js from Mozilla's PDF.js releases
// 2. Place it in this location
// 3. Or use a CDN version

console.log(
  "PDF.js worker placeholder - replace with actual worker file for production use"
);

// For now, we'll use the CDN version by updating the worker source
if (typeof window !== "undefined" && window.pdfjsLib) {
  window.pdfjsLib.GlobalWorkerOptions.workerSrc =
    "https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js";
}
